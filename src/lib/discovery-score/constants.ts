/** Benchmark cohort size — raise to 20 for production parity tests. */
export const DISCOVERY_COHORT_SIZE = 10;

/** Reserved key inside profile.parsedData JSON — preserved across resume PATCH merges. */
export const DISCOVERY_SCORE_CACHE_KEY = "__kimchiDiscoveryScore";

/** User-selected Hirebase job function category for Discovery Score benchmarks. */
export const DISCOVERY_BENCHMARK_CATEGORY_KEY = "discoveryBenchmarkCategory";
